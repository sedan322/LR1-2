var a, b, c, n :integer;
begin
  writeln('введите три числа'); readln(a,b,c);
  write('наименьшее число = ');
  if a > b then
  if b > c then
  write(c)
  else
  write(b)
  else 
    if b>c then
      if a>c then
        write(a)
      else
      write(b)
      else
      write(a);
end.